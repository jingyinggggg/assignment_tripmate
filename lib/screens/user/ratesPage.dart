import 'package:flutter/material.dart';

class RatesPageScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Column(
        children: <Widget>[
          Center(
            child: Text("Rates Page"),
          )
        ],
      )
    );
    
  }
}